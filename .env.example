# Copy this file to .env and fill in your values
# .env is gitignored and should never be committed

# Used with --api-key CLI flag or security: "api-key" in config.json
ERI_API_KEY=

# Used with --secure-domains CLI flag (no value) or security: "domains" in config.json
ERI_ALLOWED_DOMAINS=*.catprint.com
